version: '3.7'

services:
    webserver:
        image: rootlessaspnetcore
        container_name: rootlessaspnetcore
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - 0.0.0.0:3000:3000
